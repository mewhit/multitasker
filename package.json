{
  "name": "multitasker",
  "version": "1.0.0",
  "description": "Electron desktop app",
  "main": "dist/desktop/main.js",
  "scripts": {
    "clean:debug-log": "node scripts\\clean-debug-log.js",
    "prebuild": "npm run clean:debug-log",
    "build": "tsc -p tsconfig.json",
    "postbuild": "npm run clean:debug-log",
    "server": "node dist\\http-server\\server.js",
    "apps:start": "npm run start",
    "apps:dev": "npm run dev",
    "dev": "concurrently -k \"tsc -w -p tsconfig.json\" \"node scripts\\dev-electron.js\"",
    "start": "yarn build && electron ."
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.7.0",
    "concurrently": "^9.2.1",
    "electron": "^42.0.1",
    "typescript": "^5.9.3",
    "wait-on": "^9.0.1"
  },
  "dependencies": {
    "@slack/web-api": "^7.16.0",
    "node-window-manager": "^2.2.4"
  }
}
